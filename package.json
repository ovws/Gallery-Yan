{
  "name": "gallery-yan",
  "version": "1.0.0",
  "private": true,
  "description": "嫣 — image gallery",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "thumbs": "node scripts/gen-thumbs.js",
    "build": "npm run build:insights",
    "build:insights": "esbuild speed-insights.js --bundle --minify --outfile=speed-insights.bundle.js --format=esm"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "sharp": "^0.35.3"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.1.0",
    "sharp": "^0.35.3"
  }
}
